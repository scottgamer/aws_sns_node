{
  "name": "aws_sns_node",
  "version": "1.0.0",
  "description": "Notification project example using aws sns service ",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "babel src -d build & npm run start",
    "start": "node build/app.js"
  },
  "author": "Richard Munoz",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.528.0",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-cli": "^6.26.0"
  }
}
